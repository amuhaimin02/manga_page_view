import 'package:flutter/widgets.dart';

class MangaPageScreenView extends StatelessWidget {
  const MangaPageScreenView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
